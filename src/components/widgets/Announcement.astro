---
/**
 * Announcement bar slot — override per-page via <Fragment slot="announcement">.
 * Default: empty (no banner). Kept as a named slot for future promotions.
 */
---

<slot>
  <!-- Default: no announcement. Override with <Fragment slot="announcement"> on any page. -->
</slot>
